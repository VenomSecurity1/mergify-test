pull_request_rules:
  - name: test-template-render
    conditions:
      - "#approved-reviews-by>=1"
    actions:
      comment:
        message: |
          Test render: {{ body | get_section("## PoC", "no-section-found") }}
