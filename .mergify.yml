pull_request_rules:
  - name: auto-merge on approval
    conditions:
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: merge
queue_rules:
  - name: testing
merge_queue:
  status_comments: all
